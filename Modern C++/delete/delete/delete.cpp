#include <iostream>
#include <map>
#include <vector>
#include <list>
#include <deque>
#include <set>
#include <algorithm>
using namespace std;

// 프로젝트 주제 : delete (삭제된 함수)

class Knight
{
public:

    // 정의되지 않은 비공개(private) 함수
    
    // 오퍼레이터 함수 = delete; 

private:
    int _hp = 100;
};

int main()
{





    return 0;
}

